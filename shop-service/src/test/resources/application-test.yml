# 本配置只连接本机的隔离测试库；可通过环境变量覆盖全部连接信息。
spring:
  datasource:
    url: ${JAVAFIX_TEST_DB_URL:jdbc:mysql://127.0.0.1:3306/javafix_shop_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${JAVAFIX_TEST_DB_USERNAME:javafix_test}
    password: ${JAVAFIX_TEST_DB_PASSWORD:javafix_test}
    driver-class-name: com.mysql.cj.jdbc.Driver

