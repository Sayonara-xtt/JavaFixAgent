# 连接信息由 MySqlTestConfiguration 的 @ServiceConnection 提供。
# 无容器配置时也禁止回退到本机数据库，宁可启动失败。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:1/testcontainers_required
    username: unused
    password: unused
